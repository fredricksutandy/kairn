/**
 * Build order step 9. Nothing here yet, and nothing before the Android gate.
 *
 * Until then the config is a hardcoded object. See docs/02-architecture.md for
 * the table shapes this package will migrate.
 */
export {};
