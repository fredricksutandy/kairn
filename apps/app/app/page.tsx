/**
 * Placeholder. The real builder UI is step 8 and is gated on step 7 — a real
 * composition surviving on a real mid-range Android. CLAUDE.md: never build the
 * builder before the gate passes.
 */
export default function Page() {
  return (
    <main>
      <h1>Kairn builder</h1>
      <p>Blocked on the Android gate. See docs/04-roadmap.md.</p>
    </main>
  );
}
